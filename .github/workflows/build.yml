name: Build image

on:
  push:

jobs:
  build_image:
    name: Build image with buildah
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4
      - name: Run build
        run: buildah build -f Containerfile -t tbn-fedora-toolbox-python
